[Unit]
Description=gunicorn daemon
After=network.target

[Service]
User=nginx
Group=nginx
WorkingDirectory={{ app_dir }}/{{ app_name }}
ExecStart=/usr/bin/gunicorn --workers 3 --bind unix:{{ socket_dir }}/{{ app_name }}.sock {{ app_name }}.wsgi:application

[Install]
WantedBy=multi-user.target
